%%%%%%%%
\usepackage{amsmath} %数学記号
\usepackage{amsfonts} %N,Z,Q,R,C等
\usepackage{theorem} %定理
\usepackage{fancybox} %枠
\usepackage{ascmac} %枠2 \itembox
\usepackage{bm} %ベクトル

%%%%%%%%%%
%定義の書式設定
%%%%%%%%%%
\theoremstyle{break}
\newtheorem{axiom}{Axiom}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{proof}{Proof.}
\renewcommand\theproof{}
\newtheorem{nproof}{Proof.}
\newtheorem{POtheorem}[nproof]{Proof of Theorem}
\newtheorem{POlemma}[nproof]{Proof of Lemma}
\newtheorem{POcorollary}[nproof]{Proof of Corollary}
\newtheorem{POproposition}[nproof]{Proof of Proposition}

%%%%%%%%%%
%記号
%%%%%%%%%%

%集合
\newcommand\N{\mathbb{N}}
\newcommand\Z{\mathbb{Z}}
\newcommand\Q{\mathbb{Q}}
\newcommand\R{\mathbb{R}}
\newcommand\C{\mathbb{C}}
%矢印
\newcommand\To{\Rightarrow}
\newcommand\ot{\leftarrow}
\newcommand\oT{\Leftarrow}
\newcommand\tot{\leftrightarrow}
\newcommand\ToT{\Leftrightarrow}
%定義式
\newcommand\defarrow{\overset{\operatorname{def}}{\Leftrightarrow}}
\newcommand\defeq{\overset{\operatorname{def}}{=}}
%関数
\newcommand\id{\operatorname{id}}
%虚部と像が衝突するので再定義
\let\im\Im
\renewcommand\Im{\operatorname{Im}}
%div
\let\Div\div
\renewcommand\div{\mathbin{\operatorname{div}}}
%mod
\let\Mod\mod
\renewcommand\mod\bmod
%論理演算子
\let\Vee\vee
\renewcommand\vee{\mathrel{\Vee}}
\let\Wedge\wedge
\renewcommand\wedge{\mathrel{\Wedge}}

\newcommand\power{\mathcal{P}}
\newcommand\powerset[1]{\power(#1)}
\newcommand\sgn{\operatorname{sgn}}
\newcommand\argmax{\mathop{\operatorname{arg\,max}}\limits}
\newcommand\argmin{\mathop{\operatorname{arg\,min}}\limits}
%数列
\newcommand\seq[3]{#1_{#2},\,\ldots,\,#1_{#3}}
%ベクトル成分表示
\newcommand\vseq[2]{\begin{pmatrix}#1_1\\ \vdots\\ #1_{#2} \end{pmatrix}}
\newcommand\vseqz[2]{\begin{pmatrix}#1_0\\ \vdots\\ #1_{#2} \end{pmatrix}}
\newcommand\hseq[2]{(\seq{#1}{1}{#2})}
\newcommand\hseqz[2]{(\seq{#1}{0}{#2})}
%高階関数ver
%
%
%
%有限集合表示
\newcommand\fset[2]{\{\seq{#1}{1}{#2}\}}
\newcommand\ordinal[1]{\if 0#1 \emptyset
					\else {\ifx 1#1 \{1\}
						\else {\ifx 2#1 \{1,\,2\}
							\else {\ifx 3#1 \{1,\,2,\,3\}
								\else \{1,\,\ldots,\,#1\}\fi}
							\fi}
						\fi}
					\fi}
%行列成分表示
\newcommand\mat[3]{\begin{matrix}#1_{1,1}&\cdots&#1_{1,#3}\\ \vdots&&\vdots\\ #1_{#2,1}&\cdots&#1_{#2,#3}\end{matrix}}
\newcommand\pmat[3]{\begin{pmatrix}#1_{1,1}&\cdots&#1_{1,#3}\\ \vdots&&\vdots\\ #1_{#2,1}&\cdots&#1_{#2,#3}\end{pmatrix}}

%%%%%%%%%%
%ベクトル
%%%%%%%%%%

%衝突回避

%アクセント記号
\let\accentb\b
\let\accentc\c
\let\accentd\d
\let\escapei\i
\let\escapej\j
\let\escapel\l
\let\accento\o
\let\accentr\r
\let\accentt\t
\let\accentu\u
\let\accentv\v
%用途不明
\let\escapea\a
\let\escapek\k

%%%%%%%%%%
\renewcommand\a{\bm{a}}
\renewcommand\b{\bm{b}}
\renewcommand\c{\bm{c}}
\renewcommand\d{\bm{d}}
\newcommand\e{\bm{e}}
\newcommand\f{\bm{f}}
\newcommand\g{\bm{g}}
\newcommand\h{\bm{h}}
\renewcommand\i{\bm{i}}
\renewcommand\j{\bm{j}}
\renewcommand\k{\bm{k}}
\renewcommand\l{\bm{l}}
\newcommand\m{\bm{m}}
\newcommand\n{\bm{n}}
\renewcommand\o{\bm{o}}
\newcommand\p{\bm{p}}
\newcommand\q{\bm{q}}
\renewcommand\r{\bm{r}}
\newcommand\s{\bm{s}}
\renewcommand\t{\bm{t}}
\renewcommand\u{\bm{u}}
\renewcommand\v{\bm{v}}
\newcommand\w{\bm{w}}
\newcommand\x{\bm{x}}
\newcommand\y{\bm{y}}
\newcommand\z{\bm{z}}

